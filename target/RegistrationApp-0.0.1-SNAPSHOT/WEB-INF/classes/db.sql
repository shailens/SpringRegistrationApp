
CREATE TABLE ROLE (ID NUMBER NOT NULL,NAME VARCHAR2(200) DEFAULT NULL,PRIMARY KEY (ID));               
INSERT INTO ROLE VALUES(1,'ROLE_USER');         
CREATE TABLE USER_CRED (ID NUMBER NOT NULL,USERNAME VARCHAR2(200),PASSWORD VARCHAR2(200),PRIMARY KEY (ID));

CREATE TABLE USER_ROLE (USER_ID NUMBER,ROLE_ID NUMBER ,PRIMARY KEY(USER_ID,ROLE_ID),
 FOREIGN KEY (USER_ID) REFERENCES USER_CRED(ID),
 FOREIGN KEY (ROLE_ID) REFERENCES ROLE(ID));